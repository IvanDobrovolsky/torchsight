{
  "name": "site",
  "type": "module",
  "version": "1.0.0-rc8",
  "scripts": {
    "prebuild": "node scripts/sync-version.mjs",
    "dev": "astro dev",
    "build": "node scripts/sync-version.mjs && astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "astro": "astro",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@tailwindcss/vite": "^4.2.1",
    "astro": "^5.17.1",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
